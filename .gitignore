# --- Dependencias ---
node_modules/

# --- Variables de entorno (NUNCA subir credenciales a git) ---
.env
.env.*
!.env.example

# --- Builds y artefactos generados ---
dist/
build/
.next/
out/
coverage/
*.tsbuildinfo

# --- Prisma (cliente generado) ---
generated/

# --- Sistema operativo / editor ---
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# --- Logs ---
*.log
npm-debug.log*
pnpm-debug.log*

# --- Respaldos de base de datos (contienen datos de clientes, nunca a git) ---
/backups/

# --- Documentos internos (costos, márgenes, precios: nunca a git) ---
FixTrack_Pro_Precios_y_Monetizacion.pdf

# --- Firma digital y llave de firma de la app (nunca a git) ---
firma_*.png
*.jks
key.properties

# --- Configuración local del agente ---
.claude/
